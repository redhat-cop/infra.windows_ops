---
# =============================================================================
# Windows Server 2025 DISA STIG V1R1 Benchmark Data
# Microsoft Windows Server 2025 Security Technical Implementation Guide
# Release Date: January 23, 2026
# =============================================================================

# Metadata for audit trail
windows_manage_stig_benchmark_metadata:
  name: "Microsoft Windows Server 2025 Security Technical Implementation Guide"
  version: "V1R1"
  release_date: "2026-01-23"
  total_rules: 295
  deprecated_rules: []
  new_rules:
    - V-254501  # Hotpatching configuration
    - V-254502  # Next-Gen Credential Guard
    - V-254503  # AD 32k pages support

# Version-specific feature flags
windows_manage_stig_version_features:
  has_native_laps: true
  has_dns_over_https: true
  has_smb_over_quic: true
  has_hotpatching: true
  has_nextgen_credential_guard: true
  smb_signing_default_enabled: true
  has_tls_13_default: true
  has_azure_arc_integration: true
  has_ad_32k_pages: true

# OS-specific default values (override role defaults where needed)
windows_manage_stig_version_defaults:
  # SMB signing already enabled by default in 2025
  smb_signing_enforcement_required: false

  # LAPS is native in 2025
  laps_installation_method: "native"

  # TLS 1.3 is default in 2025
  tls_minimum_version: "1.3"

# 2025-specific control overrides
windows_manage_stig_2025_control_overrides:
  - control_id: "V-254XXX"
    reason: "SMB signing enabled by default in 2025"
    action: "verify_only"
